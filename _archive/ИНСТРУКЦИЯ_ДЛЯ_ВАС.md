# 🎯 ИНСТРУКЦИЯ ПО ДЕПЛОЮ - lk.housler.ru

## ✅ ВСЁ ГОТОВО К ДЕПЛОЮ!

Проект полностью готов. Git инициализирован, все файлы закоммичены.

---

## 📋 ЧТО НУЖНО СДЕЛАТЬ

### ШАГ 1: Создать репозиторий на GitHub (2 минуты)

1. Откройте в браузере: https://github.com/new
2. Заполните форму:
   - **Repository name**: `lk-housler` (или любое другое название)
   - **Description**: `Agent Deal Platform - lk.housler.ru`
   - **Visibility**: выберите **Private** (рекомендуется)
   - **НЕ** ставьте галочки на "Add a README", "Add .gitignore", "Choose a license"
3. Нажмите **"Create repository"**
4. GitHub покажет инструкции - **НЕ ЗАКРЫВАЙТЕ ЭТУ СТРАНИЦУ**

### ШАГ 2: Загрузить код на GitHub (1 минута)

Скопируйте и выполните эти команды в терминале:

```bash
cd /Users/fatbookpro/Desktop/lk

# ЗАМЕНИТЕ YOUR_USERNAME на ваш GitHub username из адреса репозитория
git remote add origin git@github.com:YOUR_USERNAME/lk-housler.git

# Отправка кода
git push -u origin main
```

**Если у вас нет SSH ключа для GitHub**, используйте HTTPS:

```bash
# ЗАМЕНИТЕ YOUR_USERNAME
git remote add origin https://github.com/YOUR_USERNAME/lk-housler.git
git push -u origin main
# Введите ваш GitHub username и Personal Access Token
```

### ШАГ 3: Деплой на сервер (30-40 минут)

Теперь откройте файл **DEPLOY_INSTRUCTIONS.md** и следуйте инструкциям.

Или выполните команды ниже:

#### 3.1. Подключение к серверу

```bash
ssh -i ~/.ssh/id_housler root@91.229.8.221
```

#### 3.2. Клонирование проекта

```bash
cd /var/www

# ЗАМЕНИТЕ YOUR_USERNAME на ваш GitHub username
git clone git@github.com:YOUR_USERNAME/lk-housler.git lk.housler.ru

cd lk.housler.ru
```

#### 3.3. Генерация секретных ключей

```bash
# Генерация ключей
./scripts/generate-keys.sh

# Скопируйте вывод - эти ключи нужны для .env
```

#### 3.4. Создание .env файла

```bash
nano .env
```

Вставьте это содержимое (замените значения на сгенерированные):

```env
# Database
DB_NAME=lk_housler
DB_USER=lk_user
DB_PASSWORD=<ВСТАВЬТЕ_СГЕНЕРИРОВАННЫЙ_ПАРОЛЬ>

# JWT Secret
JWT_SECRET=<ВСТАВЬТЕ_СГЕНЕРИРОВАННЫЙ_JWT_SECRET>

# Encryption Key
ENCRYPTION_KEY=<ВСТАВЬТЕ_СГЕНЕРИРОВАННЫЙ_ENCRYPTION_KEY>

# SMS.RU (ваш API ID)
SMS_PROVIDER=sms_ru
SMS_RU_API_ID=<ВАШ_SMS_RU_API_ID>
SMS_TEST_MODE=false

# MinIO
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=<ВСТАВЬТЕ_СГЕНЕРИРОВАННЫЙ_MINIO_PASSWORD>

# Next.js
NEXT_PUBLIC_API_URL=https://lk.housler.ru
```

Сохраните: `Ctrl+X`, затем `Y`, затем `Enter`

#### 3.5. Запуск приложения

```bash
# Создание директорий
mkdir -p logs/backend logs/nginx backups

# Запуск Docker контейнеров (это займет 5-10 минут)
docker-compose -f docker-compose.prod.yml up -d --build

# Ожидание запуска
sleep 30

# Проверка статуса
docker-compose -f docker-compose.prod.yml ps
```

#### 3.6. Настройка Nginx

```bash
# Копирование конфигурации
cp nginx/lk.housler.ru.conf /etc/nginx/sites-available/lk.housler.ru
ln -s /etc/nginx/sites-available/lk.housler.ru /etc/nginx/sites-enabled/lk.housler.ru

# Удаление дефолтного конфига (если есть)
rm -f /etc/nginx/sites-enabled/default

# Проверка конфигурации
nginx -t

# Перезагрузка Nginx
systemctl reload nginx
```

#### 3.7. Получение SSL сертификата

```bash
certbot --nginx -d lk.housler.ru
```

Следуйте инструкциям certbot:
- Введите email для уведомлений
- Согласитесь с условиями (Y)
- Выберите редирект HTTP -> HTTPS: 2

#### 3.8. Проверка

```bash
# Проверка здоровья
./scripts/health-check.sh

# Проверка API
curl https://lk.housler.ru/api/health
```

Откройте в браузере: **https://lk.housler.ru**

---

## 🎉 ГОТОВО!

Если всё прошло успешно, ваше приложение работает на **https://lk.housler.ru**

---

## 📚 Документация

Все документы находятся в корне проекта:

### Главные документы:

1. **DEPLOY_INSTRUCTIONS.md** - Полная пошаговая инструкция
2. **QUICK_DEPLOY.md** - Краткая версия
3. **DEPLOYMENT.md** - Подробная документация с troubleshooting
4. **READY_TO_DEPLOY.md** - Резюме проекта

### Скрипты (в папке scripts/):

- `generate-keys.sh` - Генерация секретных ключей
- `backup.sh` - Создание backup БД и MinIO
- `restore.sh` - Восстановление из backup
- `logs.sh` - Просмотр логов
- `health-check.sh` - Проверка здоровья всех сервисов

### Автоматический деплой:

```bash
./deploy.sh
```

---

## 🆘 Если что-то пошло не так

1. Проверьте логи:
   ```bash
   ./scripts/logs.sh all
   ```

2. Проверьте статус контейнеров:
   ```bash
   docker-compose -f docker-compose.prod.yml ps
   ```

3. Проверьте здоровье:
   ```bash
   ./scripts/health-check.sh
   ```

4. Смотрите раздел Troubleshooting в **DEPLOY_INSTRUCTIONS.md**

---

## 📞 Важная информация

- **Сервер**: 91.229.8.221
- **SSH**: `ssh -i ~/.ssh/id_housler root@91.229.8.221`
- **Домен**: https://lk.housler.ru
- **Путь на сервере**: `/var/www/lk.housler.ru`

---

## 🔄 Обновление в будущем

```bash
# На сервере
cd /var/www/lk.housler.ru
git pull origin main
docker-compose -f docker-compose.prod.yml up -d --build
```

Или просто:

```bash
./deploy.sh
```

---

**Удачи с деплоем! 🚀**
